# Secure Chat — Assignment 2

This repository implements the secure chat assignment. Follow the steps in the README to set up the environment, create a root CA, issue certificates, and run the server and client.

## Quick start (Phase 0)
1. Create virtualenv and install requirements:
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt